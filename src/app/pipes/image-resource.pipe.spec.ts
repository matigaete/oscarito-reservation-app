import { ImageResourcePipe } from './image-resource.pipe';

describe('ImageResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageResourcePipe();
    expect(pipe).toBeTruthy();
  });
});
